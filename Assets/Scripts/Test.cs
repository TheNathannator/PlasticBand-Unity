using PlasticBand;
using PlasticBand.Devices;
using PlasticBand.Haptics;
using UnityEngine;
using UnityEngine.InputSystem;

// Small script for testing the APIs provided by the device classes.
// This class is persisted so it doesn't need to be created repeatedly, just modified.
// No changes to this class should be committed, aside from common routines or quality-of-life additions.

namespace PlasticBand.Test
{
    public class Test : MonoBehaviour
    {
        private void Start()
        {
        }

        private void Update()
        {
        }
    }
}